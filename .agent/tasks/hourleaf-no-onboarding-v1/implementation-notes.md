# Implementation notes

The onboarding overlay is removed at the root navigation boundary. Legacy
settings and backup fields remain unchanged so old data can still be restored.
New users can immediately add current or past-dated entries; existing users use
the supported portable backup restore flow.
